<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhu.dao.UserDao">
    <update id="updateUserInfo" parameterType="com.zhu.domain.UserInfo">
        update t_user_info
        set
        <if test="nick != null and nick != ''">
            nick = #{nick},
        </if>
        <if test="avatar != null and avatar != ''">
            avatar = #{avatar},
        </if>
        <if test="sign != null and sign != ''">
            sign = #{sign},
        </if>
        <if test="gender != null and gender != ''">
            gender = #{gender},
        </if>
        <if test="birth != null and birth != ''">
            birth = #{birth},
        </if>
        updateTime = #{updateTime}
        where
        userId = #{userId}
    </update>

    <select id="getUserByPhone" parameterType="java.lang.String" resultType="com.zhu.domain.User">
        select *
        from t_user
        where phone = #{phone}
    </select>

    <select id="getUserById" parameterType="java.lang.Long" resultType="com.zhu.domain.User">
        select *
        from t_user
        where id = #{userId}
    </select>

    <select id="getUserInfoByUserId" parameterType="java.lang.Long" resultType="com.zhu.domain.UserInfo">
        select *
        from t_user_info
        where userId = #{userId}
    </select>

    <insert id="addUser" parameterType="com.zhu.domain.User" useGeneratedKeys="true" keyProperty="id">
        insert into t_user(phone,
                           email,
                           password,
                           salt,
                           createTime)
        values (#{phone},
                #{email},
                #{password},
                #{salt},
                #{createTime})
    </insert>
    <insert id="addUserInfo" parameterType="com.zhu.domain.UserInfo">
        insert into t_user_info(userId,
                                nick,
                                avatar,
                                sign,
                                gender,
                                birth,
                                createTime)
        values (#{userId},
                #{nick},
                #{avatar},
                #{sign},
                #{gender},
                #{birth},
                #{createTime})
    </insert>
</mapper>